# Production dependencies for Plant Disease Detector
# Minimal set for deployment (excludes dev/test dependencies)

# Core dependencies
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
opencv-python-headless>=4.5.0,<5.0.0  # Headless version for production
Pillow>=9.0.0,<11.0.0
tqdm>=4.62.0,<5.0.0
pyyaml>=6.0,<7.0

# Image augmentation
albumentations>=1.3.0,<2.0.0

# Deep learning (PyTorch)
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
timm>=0.9.0,<1.0.0

# Web UI (optional - only if deploying Streamlit)
streamlit>=1.20.0,<2.0.0

# API server (optional - only if deploying FastAPI)
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0,<1.0.0
python-multipart>=0.0.6

# Utilities
click>=8.0.0,<9.0.0

# Production enhancements
gunicorn>=21.0.0  # WSGI server
prometheus-fastapi-instrumentator>=6.0.0  # Metrics
python-json-logger>=2.0.0,<3.0.0  # Structured logging
